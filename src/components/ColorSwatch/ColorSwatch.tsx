import { IColorSwatchProps } from './ColorSwatch.types';

import * as S from './ColorSwatch.styles';

export const ColorSwatch = (props: IColorSwatchProps): JSX.Element => {
  return (
    <S.ColorSwatch>
      <p>ColorSwatch</p>
    </S.ColorSwatch>
  );
};